{
  "scripts": {
    "format": "prettier --write app",
    "start": "http-server",
    "tailwind": "npx tailwindcss -i ./style.css -o ./dist/style.css --watch"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "tailwindcss": "^3.1.8"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
